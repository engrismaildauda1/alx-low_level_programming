#include "main.h"
#include <stdio.h>

/**
 * mul - Multiply 2 integers.
 * @a:	first integer a
 * @b :	second integer b
 * Return: Return the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
